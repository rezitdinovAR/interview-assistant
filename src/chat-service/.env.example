# OpenAI API Key (required)
OPENAI_API_KEY=your_openai_api_key_here

# Redis connection (по умолчанию для docker-compose)
REDIS_URI=redis://redis:6379

# DB Service URL
# Для запуска с основным docker-compose: http://api:8080
# Для запуска отдельно (chat-service/docker-compose.yml): http://host.docker.internal:8083
DB_SERVICE_URL=http://api:8080

# RAG settings
TOP_K_DOCUMENTS=5
MAX_TOKENS=10000

# Optional: path to custom system prompt
# SYSTEM_PROMPT_PATH=/workspace/prompts/system_prompt.txt
