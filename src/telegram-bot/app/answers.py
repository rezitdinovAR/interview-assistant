PYTHON_DECORATORS = """
Декоратор в Python — это функция, которая принимает другую функцию в качестве аргумента и расширяет или изменяет ее функциональность, не изменяя при этом исходный код самой функции. Декоратор возвращает новую функцию (функцию-обертку).

### Назначение декораторов

Декораторы используются для добавления дополнительной функциональности к существующим функциям, включая:

*   Логирование вызовов функции, ее аргументов и результатов.
*   Тайминг выполнения.
*   Проверку аутентификации.
*   Кэширование результатов функции.

### Как написать собственный декоратор

Для создания собственного декоратора необходимо определить внешнюю функцию, которая принимает декорируемую функцию в качестве аргумента, и внутреннюю функцию-обертку (`wrapper`), которая содержит дополнительную логику:

```python
def my_decorator(func):
    def wrapper():
        print("Дополнительный код, который исполняется перед вызовом функции")
        func()
        print("Дополнительный код, который исполняется после вызова функции")
    return wrapper

@my_decorator
def say_hello():
    print("Привет!")

say_hello()
```

В приведенном примере `my_decorator` добавляет код до и после вызова функции `say_hello()`. Применение синтаксиса `@my_decorator` к функции `say_hello` автоматически оборачивает ее в логику декоратора.

**Пример логирования:**

Декоратор также может принимать аргументы и логировать их вместе с результатом:

```python
def logger(func):
    def wrapper(*args, **kwargs):
        result = func(*args, **kwargs)
        print(f"Called {func.__name__} with args={args} and kwargs={kwargs}. Result: {result}")
        return result
    return wrapper

@logger
def add(x, y):
    return x + y

add(1, 2)
# Output: Called add with args=(1, 2) and kwargs={}. Result: 3
```
"""
